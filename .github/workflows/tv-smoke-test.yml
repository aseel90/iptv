name: Selyro Android 9-11 Runtime Smoke

on:
  push:
    branches:
      - 'qa/full-v1-ci'
    paths-ignore:
      - 'ci-artifacts/**'
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write
  issues: write

jobs:
  runtime-smoke:
    name: runtime-smoke-api-${{ matrix.api }}
    runs-on: ubuntu-latest
    timeout-minutes: 35
    strategy:
      fail-fast: false
      matrix:
        include:
          - api: 28
            arch: x86
            ram: 1024M
            heap: 192
            device_label: Android 9 runtime / Mi TV Stick generation
          - api: 30
            arch: x86_64
            ram: 1536M
            heap: 256
            device_label: Android 11 runtime / Xiaomi TV Stick 4K generation
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - name: Install Gradle 8.13
        run: |
          curl -fsSL https://services.gradle.org/distributions/gradle-8.13-bin.zip -o /tmp/gradle.zip
          unzip -q /tmp/gradle.zip -d /tmp
          echo "/tmp/gradle-8.13/bin" >> "$GITHUB_PATH"
      - name: Enable KVM
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm
      - name: Boot Android and verify Selyro startup
        id: runtime
        uses: ReactiveCircus/android-emulator-runner@v2.38.0
        with:
          api-level: ${{ matrix.api }}
          target: google_apis
          arch: ${{ matrix.arch }}
          cores: 2
          ram-size: ${{ matrix.ram }}
          heap-size: ${{ matrix.heap }}
          emulator-options: -no-window -no-snapshot -noaudio -no-boot-anim -gpu swiftshader_indirect
          disable-animations: true
          script: |
            set -euxo pipefail
            capture() {
              adb logcat -d -v threadtime > runtime-logcat-api-${{ matrix.api }}.txt 2>&1 || true
              adb shell dumpsys activity activities > runtime-activities-api-${{ matrix.api }}.txt 2>&1 || true
              adb shell dumpsys package com.selyro.tv > runtime-package-api-${{ matrix.api }}.txt 2>&1 || true
            }
            trap capture EXIT

            gradle :app:assembleDebug --stacktrace
            adb install -r app/build/outputs/apk/debug/app-debug.apk

            adb shell cmd package resolve-activity --brief -a android.intent.action.MAIN -c android.intent.category.LAUNCHER com.selyro.tv | tee /tmp/launcher.txt
            grep -q 'com.selyro.tv/.MainActivity' /tmp/launcher.txt
            adb shell cmd package resolve-activity --brief -a android.intent.action.MAIN -c android.intent.category.LEANBACK_LAUNCHER com.selyro.tv | tee /tmp/leanback.txt
            grep -q 'com.selyro.tv/.MainActivity' /tmp/leanback.txt

            adb logcat -c
            adb shell am force-stop com.selyro.tv
            adb shell am start -W -a android.intent.action.MAIN -c android.intent.category.LEANBACK_LAUNCHER -p com.selyro.tv | tee /tmp/start.txt
            grep -q 'Status: ok' /tmp/start.txt
            sleep 10

            adb shell pidof com.selyro.tv | grep -Eq '[0-9]+'
            adb logcat -d -v brief | grep -q 'SelyroStartup.*compose:first-composition:ok'

            if ! adb shell dumpsys activity activities | grep -q 'com.selyro.tv/.MainActivity'; then
              if ! adb shell dumpsys window windows | grep -q 'com.selyro.tv'; then
                echo 'Selyro is not foreground after 10 seconds'
                exit 1
              fi
            fi

            if adb logcat -d -v brief | grep -A 40 -B 8 -E 'FATAL EXCEPTION|Process: com.selyro.tv' ; then
              echo 'Selyro crashed during runtime smoke test'
              exit 1
            fi

            adb shell am force-stop com.selyro.tv
            adb logcat -c
            adb shell am start -W -a android.intent.action.MAIN -c android.intent.category.LAUNCHER -p com.selyro.tv | tee /tmp/start-standard.txt
            grep -q 'Status: ok' /tmp/start-standard.txt
            sleep 5
            adb shell pidof com.selyro.tv | grep -Eq '[0-9]+'
            adb logcat -d -v brief | grep -q 'SelyroStartup.*compose:first-composition:ok'
            if adb logcat -d -v brief | grep -A 40 -B 8 -E 'FATAL EXCEPTION|Process: com.selyro.tv'; then
              exit 1
            fi
      - name: Report result to PR
        if: always()
        env:
          GH_TOKEN: ${{ github.token }}
          OUTCOME: ${{ steps.runtime.outcome }}
          API_LEVEL: ${{ matrix.api }}
          DEVICE_LABEL: ${{ matrix.device_label }}
        run: |
          pr=$(gh pr list --repo "$GITHUB_REPOSITORY" --head qa/full-v1-ci --state open --json number --jq '.[0].number // empty')
          if [ -z "$pr" ]; then exit 0; fi
          if [ "$OUTCOME" = "success" ]; then
            printf '%s\n%s\n%s\n%s\n' \
              "### Selyro runtime startup API ${API_LEVEL}: PASS" \
              "Commit: ${GITHUB_SHA}" \
              "Profile: ${DEVICE_LABEL}" \
              'Verified fresh install, LEANBACK launch, standard launcher launch, first Compose composition, foreground survival, and no fatal crash.' > /tmp/runtime-result.md
          else
            {
              echo "### Selyro runtime startup API ${API_LEVEL}: FAIL"
              echo "Commit: ${GITHUB_SHA}"
              echo "Profile: ${DEVICE_LABEL}"
              echo
              echo 'Relevant startup/crash log:'
              echo '```text'
              file="runtime-logcat-api-${API_LEVEL}.txt"
              if [ -s "$file" ]; then
                grep -A 55 -B 10 -E 'SelyroStartup|FATAL EXCEPTION|AndroidRuntime|Process: com.selyro.tv|InflateException|Resources\$NotFoundException|NoSuchMethodError|ClassNotFoundException|IllegalStateException|OutOfMemoryError|VerifyError' "$file" | head -n 240 || tail -n 160 "$file"
              else
                echo 'No app logcat was captured; emulator setup failed before the app test script started.'
              fi
              echo '```'
            } > /tmp/runtime-result.md
          fi
          gh pr comment "$pr" --repo "$GITHUB_REPOSITORY" --body-file /tmp/runtime-result.md
      - name: Upload diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: selyro-runtime-api-${{ matrix.api }}
          path: |
            runtime-logcat-api-${{ matrix.api }}.txt
            runtime-activities-api-${{ matrix.api }}.txt
            runtime-package-api-${{ matrix.api }}.txt
          if-no-files-found: ignore
